[package]
name = "jetstreamer-historical-protocol"
version = "0.1.0"
edition = "2018"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
bincode = "=1.2.1"
serde = { version = "=1.0.105", features = ["derive"] }
